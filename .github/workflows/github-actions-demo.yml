name: CI

on: [push]

jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Symfony
        working-directory: ${{ inputs.working_directory }}
        run: gradle clean test
       
    
