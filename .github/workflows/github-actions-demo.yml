name: CI

on: [push]

jobs:
  test-working-directory:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - run: cd  ${{ inputs.working_directory }}
      - run: gradle clean test 
       
    
